version: 2

models:
  - name: obt_track_review
    description: The track review fact table combined with all of its outrigger dimensions into one bit table. Transaction fact table with one row per a track reviews
