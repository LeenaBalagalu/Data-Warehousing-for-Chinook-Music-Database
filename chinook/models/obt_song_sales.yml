version: 2

models:
  - name: obt_song_sales
    description: The song sales fact table combined with all of its outrigger dimensions into one bit table. Transaction fact table with one row per monthly orders of a single song 
