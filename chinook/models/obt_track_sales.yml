version: 2

models:
  - name: obt_track_sales
    description: The track sales fact table combined with all of its outrigger dimensions into one bit table. periodic snapshot fact table with one row per track sales 
