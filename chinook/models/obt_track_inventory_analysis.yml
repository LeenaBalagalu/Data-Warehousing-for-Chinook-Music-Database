version: 2

models:
  - name: obt_track_inventory_analysis
    description: The track inventory fact table combined with all of its outrigger dimensions into one bit table. Transaction fact table with one row per each track inventory
